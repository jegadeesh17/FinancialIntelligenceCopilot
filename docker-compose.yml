services:
  app:
    build: .
    container_name: financial-compliance-rag
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data/raw_pdfs:/app/data/raw_pdfs
      - ./data/chroma_db:/app/data/chroma_db
    restart: unless-stopped
